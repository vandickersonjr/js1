prompt('What is your favorite color?')

let favColor = prompt('give your fav', '...')

switch (favColor.upperLowerCase()) //{normalize the data
  case 'blue':
      alert('${favColor} is my favorite tool!');
    break;
  default:
      alert('${myName} not a valid options!')

}
